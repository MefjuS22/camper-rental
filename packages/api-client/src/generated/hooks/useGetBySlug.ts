/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type {
  GetBySlugQueryResponse,
  GetBySlugPathParams,
} from "../models/GetBySlug.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const getBySlugQueryKey = (slug: GetBySlugPathParams["slug"]) =>
  [{ url: "/api/v1/public/cms/posts/:slug", params: { slug: slug } }] as const;

export type GetBySlugQueryKey = ReturnType<typeof getBySlugQueryKey>;

/**
 * @description Returns single published CMS post by slug.
 * @summary Get published post by slug
 * {@link /api/v1/public/cms/posts/:slug}
 */
export async function getBySlug(
  slug: GetBySlugPathParams["slug"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetBySlugQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: `/api/v1/public/cms/posts/${slug}`,
    ...requestConfig,
  });
  return res.data;
}

export function getBySlugQueryOptions(
  slug: GetBySlugPathParams["slug"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = getBySlugQueryKey(slug);
  return queryOptions<
    GetBySlugQueryResponse,
    ResponseErrorConfig<Error>,
    GetBySlugQueryResponse,
    typeof queryKey
  >({
    enabled: !!slug,
    queryKey,
    queryFn: async ({ signal }) => {
      return getBySlug(slug, { ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Returns single published CMS post by slug.
 * @summary Get published post by slug
 * {@link /api/v1/public/cms/posts/:slug}
 */
export function useGetBySlug<
  TData = GetBySlugQueryResponse,
  TQueryData = GetBySlugQueryResponse,
  TQueryKey extends QueryKey = GetBySlugQueryKey,
>(
  slug: GetBySlugPathParams["slug"],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetBySlugQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? getBySlugQueryKey(slug);

  const query = useQuery(
    {
      ...getBySlugQueryOptions(slug, config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
