/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type { ListAllQueryResponse } from "../models/ListAll.ts";
import { queryOptions, useQuery } from "@tanstack/react-query";

export const listAllQueryKey = () =>
  [{ url: "/api/v1/admin/reservations" }] as const;

export type ListAllQueryKey = ReturnType<typeof listAllQueryKey>;

/**
 * @description Returns full reservations list for admin panel.
 * @summary List all reservations
 * {@link /api/v1/admin/reservations}
 */
export async function listAll(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    ListAllQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({ method: "GET", url: `/api/v1/admin/reservations`, ...requestConfig });
  return res.data;
}

export function listAllQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const queryKey = listAllQueryKey();
  return queryOptions<
    ListAllQueryResponse,
    ResponseErrorConfig<Error>,
    ListAllQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return listAll({ ...config, signal: config.signal ?? signal });
    },
  });
}

/**
 * @description Returns full reservations list for admin panel.
 * @summary List all reservations
 * {@link /api/v1/admin/reservations}
 */
export function useListAll<
  TData = ListAllQueryResponse,
  TQueryData = ListAllQueryResponse,
  TQueryKey extends QueryKey = ListAllQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        ListAllQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {},
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...resolvedOptions } = queryConfig;
  const queryKey = resolvedOptions?.queryKey ?? listAllQueryKey();

  const query = useQuery(
    {
      ...listAllQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient,
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
